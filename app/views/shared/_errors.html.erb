<div id="error_explanation"><% if errors.any? -%>
  <h2>The form contains <%= pluralize(errors.count, "error") %>.</h2>
  <ul>
  <% errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end -%></div>
